export enum NotesSortKey {
  LAST_UPDATED = 'lastUpdated',
  TITLE = 'title',
  CREATED_DATE = 'created_date',
}

export enum MenuType {
  NOTES = 'Notes',
  FAVORITE = 'Favorites',
  TRASH = 'Trash',
  FOLDER = 'Folder',
}